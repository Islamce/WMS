node_modules
npm-debug.log
data
*.db
*.db-shm
*.db-wal
.env
.git
.github
wms flutter application
tests
*.md

# Tenant data and secrets must never be baked into the image. Each tenant's
# database is bind-mounted at run time instead.
tenants
